# 🌐 CDN IP Scanner (Cloudflare & Fastly)

یک ابزار پایتونی برای **اسکن، تست و انتخاب IPهای سالم و سریع CDN**  
پشتیبانی از:

- ✅ Cloudflare
- ✅ Fastly

همه چیز از طریق یک **منوی ساده (main.py)** اجرا می‌شود.

---

## ✨ امکانات

### 🔹 Cloudflare IP Scanner
- Ping و Latency
- تست Download / Upload
- شناسایی IPهای سالم، کند و غیرفعال

### 🔹 Fastly IP Scanner
- دریافت رنج‌های رسمی Fastly
- تست TCP اتصال به پورت 443
- تست SSL/TLS با SNI
- بررسی HTTP Response
- تخمین سرعت دانلود
- خروجی TXT و CSV

---

## 📁 ساختار پروژه

.
├── main.py # منوی انتخاب اسکنر
├── start.py # Cloudflare IP Scanner
├── fastly.py # Fastly IP Scanner
├── README.md
└── outputs/ # (اختیاری) محل ذخیره خروجی‌ها


---

## ⚙️ پیش‌نیازها

- Python **3.9+**
- قابل اجرا روی:
  - Windows
  - Linux
  - macOS

در صورت نیاز به کتابخانه اضافی:
```bash
pip install requests
🚀 نحوه اجرا
اجرای منوی اصلی
python main.py
گزینه‌ها
1 → Cloudflare IP Scanner
2 → Fastly IP Scanner
0 → خروج
🧠 توضیح main.py
فایل main.py نقش لانچر مرکزی را دارد و اسکریپت‌ها را جداگانه اجرا می‌کند:

Cloudflare:

python start.py
Fastly:

python fastly.py
این روش باعث می‌شود:

هر اسکنر مستقل اجرا شود

خطاها واضح‌تر باشند

اجرای مجدد راحت باشد

📤 خروجی‌ها
Fastly Scanner
alive_fastly.txt
IPهای سالم + زمان اتصال + سرعت

dead_fastly.txt
IPهای ناموفق + دلیل

fastly_results.csv
گزارش کامل برای تحلیل

Cloudflare Scanner
فایل‌های خروجی بسته به پیاده‌سازی (IP سالم، کند، مرده)

🔧 تنظیمات مهم (Fastly)
در فایل fastly.py:

TEST_HOST = "www.github.com"
SAMPLES_PER_CIDR_V4 = 200
CONCURRENCY = 200
📌 نکته مهم:
برای بهترین نتیجه، TEST_HOST را روی دامنه‌ی واقعی خودتان که پشت Fastly است تنظیم کنید.

⚠️ نکات مهم
بسیاری از CDNها Ping (ICMP) را مسدود می‌کنند

TLS و HTTP معیار دقیق‌تری برای سالم بودن IP هستند

نتایج به ISP، لوکیشن و شرایط شبکه بستگی دارد

🛠 ایده‌ها برای توسعه
 اضافه کردن requirements.txt

 اضافه کردن CLI Arguments

 خروجی JSON یکپارچه

 انتخاب خودکار بهترین IP

 پشتیبانی IPv6 کامل

 GitHub Actions

📜 License
MIT License
استفاده آزاد، بدون هیچ تضمینی 🙂


---

اگر دوست داری:
- نسخه **انگلیسی**
- یا README حرفه‌ای‌تر (با badge، screenshot، example output)
- یا توضیح مخصوص استفاده‌های خاص (VPN / Proxy / CDN routing)

بگو تا همون رو برات بسازم 👌